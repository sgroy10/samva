# Samva needs both Node.js and Python
[phases.setup]
nixPkgs = ["nodejs_22", "python312", "curl"]

[phases.install]
cmds = [
    "cd bridge && npm install --production",
    "cd api && pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"
